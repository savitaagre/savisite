version: 1.0

indices:
  default:
    include:
      
    exclude:
    target: '/query-index'
    properties:
      title:
        select: head > meta[property="og:title"]
        value: |
          attribute(e1, 'content')
      description:      
        select: head > meta[property="og:description"]
        value: |
          attribute(e1, 'content')